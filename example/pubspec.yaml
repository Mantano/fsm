name: fsm_example
description: Example
#version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  mno_fsm:
    path: ..

dev_dependencies:
  build_runner: ^2.4.4
  flutter_lints: ^2.0.1
  test: ^1.18.2
